package game.core;

import java.util.ArrayList;
import java.util.List;

public class AddEventListener {
    List<String> events;
    public AddEventListener(String event) {
        events.add(event);
    }
}
